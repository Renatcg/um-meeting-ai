[project]
name = "um-meeting-ai-agent"
version = "0.1.0"
description = "Jarvis LiveKit Agent for UM Meeting AI"
requires-python = ">=3.11"
dependencies = [
  "livekit-agents[openai]~=1.5",
  "aiohttp>=3.10.0",
  "python-dotenv>=1.0.1"
]

[tool.setuptools]
py-modules = ["agent", "prompts"]
